linters:
    disable-all: true # Disable everything first
    enable:
        - errcheck
        - gosimple
        - govet
        - ineffassign
        - staticcheck
        - unused
        # Gosec and gocritic sometimes cause issues
        - gosec
        - gocritic
        - nilerr
        - bodyclose
linters-settings:
    gocritic:
        enabled-tags:
            - performance
            - style
            - experimental
